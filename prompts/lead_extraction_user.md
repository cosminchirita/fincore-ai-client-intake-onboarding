Extract and classify the following lead-intake object.

```json
{{ intake_payload }}
```

The result is advisory and will be validated against the published JSON Schema before it can affect workflow state.
